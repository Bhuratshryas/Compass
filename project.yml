name: Compass
options:
  bundleIdPrefix: com.camoulabs
  deploymentTarget:
    iOS: "26.0"
  projectFormat: xcode15_0
targets:
  Compass:
    type: application
    platform: iOS
    deploymentTarget: "26.0"
    sources:
      - path: Compass
        type: group
    settings:
      base:
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        GENERATE_INFOPLIST_FILE: true
        INFOPLIST_KEY_CFBundleDisplayName: "Compass - Local AI"
        PRODUCT_NAME: "Compass - Local AI"
        INFOPLIST_KEY_NSPhotoLibraryUsageDescription: "Compass - Local AI uses photos you choose so you can ask questions about them. We do not store or send your images anywhere."
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: true
        INFOPLIST_KEY_UILaunchScreen_Generation: true
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone: "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        PRODUCT_BUNDLE_IDENTIFIER: com.camoulabs.CompassLocal
